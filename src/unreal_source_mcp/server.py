"""MCP server skeleton — tool registration and startup."""

import logging

log = logging.getLogger(__name__)


def main() -> None:
    """Entry point. TODO: wire up MCP server + tools."""
    log.info("unreal-source-mcp starting...")
    print("unreal-source-mcp v0.1.0 — not yet implemented")
